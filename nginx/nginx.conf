events {}

http {
    upstream users {
        server user-service:3001;
    }
    upstream auth {
        server auth-service:3002;
    }
    upstream address {
        server address-service:3003;
    }
    upstream search {
        server search-service:3004;
    }
    upstream products {
        server product-service:3005;
    }
    upstream cart {
        server cart-service:3006;
    }
    upstream transaction {
        server transaction-service:3007;
    }
    upstream payment {
        server payment-service:3008;
    }
    


    server {
        listen 80;
        server_name localhost;

        location /api/users/ {
            proxy_pass http://users;
        }
        location /api/auth/ {
            proxy_pass http://auth;
        }
        location /api/address/ {
            proxy_pass http://address;
        }
        location /api/search {
            proxy_pass http://search;
        }
        location /api/products {
            proxy_pass http://products;
        }
        location /api/cart {
            proxy_pass http://cart;
        }
        location /api/transaction {
            proxy_pass http://transaction;
        }
        location /api/payment {
            proxy_pass http://payment;
        }
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;


        
        location /health {
            return 200 'ok';
        }
    }
}
